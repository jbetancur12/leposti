{
  "kind": "collectionType",
  "collectionName": "products",
  "info": {
    "name": "product",
    "description": ""
  },
  "options": {
    "increments": true,
    "timestamps": true,
    "draftAndPublish": true
  },
  "attributes": {
    "nombre": {
      "type": "string",
      "required": true
    },
    "providers": {
      "collection": "provider",
      "via": "products"
    },
    "orders": {
      "collection": "order",
      "via": "product"
    },
    "prices": {
      "collection": "price",
      "via": "product"
    },
    "formato": {
      "type": "richtext"
    }
  }
}
